server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # 把所有前端路由導向 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 靜態檔案照常走
    location /assets/ {
        try_files $uri =404;
    }
}